# SDK 56 usa React 19; algunas deps web de expo-router (vaul/radix) declaran
# peers de React 18. legacy-peer-deps permite instalar sin conflicto.
# No afecta el runtime Android (esas deps son sólo de la plataforma web).
legacy-peer-deps=true
